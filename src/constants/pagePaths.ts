type PagePaths = {
  [key: string]: boolean
}

export const pagePaths: PagePaths = {
  '/': true,
  '/profile': true,
  '/favourites': true,
  '/cart': true,
  '/checkout': true
  // Add any other paths and their corresponding conditions here
}
