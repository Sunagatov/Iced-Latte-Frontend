export default function createImgUrl() {
  return null
}

// I kept this comment for the future whe
// export default function createImgUrl(url: string | null): string | null {
//   if (url === 'default file' || !url) {
//     return null
//   }

//   return url
// }
