import type { Metadata } from 'next'
import CartPage from '@/features/cart/routes/CartPage'

export const metadata: Metadata = {
  title: 'Cart',
  description: 'Review the items in your Iced Latte cart.',
}

export default CartPage
